#version 330 core
layout (location = 0) in vec2 aPos;
layout (location = 1) in vec2 texCoords;

out vec2 TexCoords;

uniform mat4 model;
uniform mat4 projection;

void main()
{
    TexCoords = texCoords;
    gl_Position = vec4(aPos.x / 100.0, aPos.y / 100.0, 0.0, 1.0);
}